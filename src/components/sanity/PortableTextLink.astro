---
import { resolveLinkAttributes } from "../../lib/content/linkResolver";

const { node, ...rest } = Astro.props as any;
const { href, target, rel } = resolveLinkAttributes(node);
---

<a href={href} target={target} rel={rel} {...rest}><slot /></a>
